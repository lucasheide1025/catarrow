# 🎯 戰鬥畫面改版規格（交接文件）

> **狀態**：版式已定案（mockup 完成），**尚未實裝**。等 FREEBUFF 先做、Claude 再修細節。
> **給誰看**：Claude / CODEX / FREEBUFF 任何一個接手都要先讀這份。
> **視覺參考**：
> - 互動 mockup（含兩種狀態＋點隊友詳情）：`docs/second_brain/battle-ui-mockup.html`（用瀏覽器直接開，自包含、已內嵌真素材）
> - 線上版：https://claude.ai/code/artifact/11b10c96-c814-493c-ad3c-b272034a97ca

---

## 為什麼要改
現在的戰鬥畫面是全站最舊的一塊：大量 inline style、沒接上後來的設計系統、單調、且**不同戰鬥模式各長一樣（拼湊感）**。目標：**一套版式跨所有戰鬥模式**（單人打怪 / 組隊 / 地下城 / 世界王），直式手機、傾斜視角、四角分工、中間留白給動畫。

---

## 版式規格（定案）

**畫面方向**：直式手機（portrait）。傾斜 arena 視角＝天空→地面漸層 + 透視地格暗示景深；實裝時直接鋪各族專屬背景圖。

**分區（重要，落地時照這個擺）**：
```
┌ 頂部資訊列：所處地城 · 難度 · 第幾層 ───────────┐
│  戰鬥訊息(左, 與怪物切齊)      怪物(右上)         │
│                                                  │
│            ← 中間一大片留白 = 動畫舞台 →          │
│  ┌ 隊友格框 ┐                                     │
│  └ 玩家卡框 ┘                          功能選單    │
└──────────────────────────────────────────────────┘
```

| 區域 | 內容 | 備註 |
|------|------|------|
| **頂部資訊列** | `🏚️ 地城名 · 難度 · 第 N / 總 層` | 依實際地城/樓層動態帶入；單人打怪可只顯示「打怪」或該場資訊 |
| **左上 戰鬥訊息** | 最近 1–2 行 | 位置與右上怪物切齊（不貼頂）；只留關鍵訊息不塞滿 |
| **右上 怪物** | `MonsterSVG` 立繪 + 血條 + 名稱等級 | **沿用原本 MonsterSVG 的方框卡樣式**（使用者確認用原素材即可） |
| **左下 兩個大框**（用一個 `team-cluster` flex column 容器堆疊，左對齊、寬度一致） | 上框＝**隊友格**、下框＝**玩家卡** | 兩框務必用容器包好正確堆疊，不可各自 absolute 亂擺互相卡住 |
| **右下 功能選單** | 計分 / 藥水 / 重置 / 放棄 | 計分＝金色主按鈕(最顯眼)；放棄＝紅色放最下(避免誤觸) |
| **中間** | 留白 | 給箭矢、傷害數字、擊殺特效、貓貓演出 |

**箭數指示**：右下選單上方一排小圓點＝本回合箭數進度（打了幾箭）。

---

## 多人處理（1～8 人，關鍵）

- **每個人在自己手機上只計自己的分** → 「你」永遠是**放大的玩家卡**（左下），其他隊友壓成小格子。
- **隊友格**：疊在玩家卡**正上方**（左下一整叢，4+3 格 wrap），每格只給四件事：
  1. **HP 細條**（誰快死一眼看到）
  2. **前/後排框色**：前排=橘(`--front #ffb454`)、後排=藍(`--rear #7dd3fc`)
  3. **右上狀態點**：綠勾✓=本回合已送出、金沙漏…=等待(閃爍)、✕=陣亡(灰掉)
- **點任一隊友格 → 跳詳細資訊卡**：頭像、前/後排、HP、ATK/DEF、貓咪夥伴、本場貢獻傷害。
- 單人模式：隊友格框隱藏，只留玩家卡框。**同一版式四模式共用**，差別只在隊友格框有沒有東西。

---

## 三種畫面狀態
1. **乾淨戰鬥**（預設）：四角 + 中間留白。
2. **計分中**：點「計分」→ 背景變暗聚焦、中央由下升起輸入區逐箭輸入 → 「送出」回乾淨畫面。
   - **數字模式**＝跳數字鍵盤(X/10/9…1/M)；**靶面模式**＝中央改顯示靶面(此版式不變，靶面計分邏輯照舊)。
3. **隊友詳情**：點隊友格 → 置中彈卡，點背景/關閉收起。

---

## 用哪些現成素材（不用新增任何美術）
- 怪物：`src/components/MonsterSVG.jsx`（36 隻，`<MonsterSVG id size />`）
- 貓咪頭像：`src/components/cat/CatSVG.jsx`（9 隻真實貓照 `/cats/*.webp`，有 pixel fallback）
- 背景：`public/ui/battle-bg/bg_{family}_{1-6}.webp`（六族各有專屬）＋ `public/ui/dungeon-bg.webp`
- 設計 token：`index.css :root`（深色 navy + 金色 accent）

---

## 相關的戰鬥邏輯修正（已完成、已上線）
- **前衛全滅＝全體判輸**：後衛無攻擊力，只剩後衛會卡死。`dungeonDb`/`partyDb` 判輸從「全員陣亡」擴充為「全員陣亡 或 (原有前衛且前衛全滅)」。見 changelog 2026-07-12。
- 放棄分流（房主解散/隊員自己離開）、增益分層（藥水戰鬥級 potionBuffs / 事件商人樓層級 buffs）也都已上線，見 changelog。

---

## 實裝計畫（照順序，別跳）
1. **先做後台教練測試畫面**：把新版式做成一個**教練後台能開的測試/預覽頁**（假資料驅動、可切單人/8人/計分/詳情），教練確認四模式都沒問題。← 使用者明確要求先驗證再實裝。
2. **落地順序**：單人打怪(`MonsterBattle`) → 組隊(`PartyBattleRoom`) → 地下城(`DungeonBattleRoom`) → 世界王(`WorldBossAttack`)。單人最單純、無多人同步，風險最低，先跑順。
3. **舊版並存**：新版穩定前不砍舊版，可切換；確認 OK 才全面換掉。
4. 抽共用元件（版式外殼、四角、team-cluster、計分覆蓋層、詳情卡）讓四模式共用，避免又變成各刻各的。

## 分工
- **FREEBUFF**：沒有流量限制，先把主體做完（測試畫面 + 落地）。
- **Claude**：流量有限，接手修細節、對數值、除錯。
- **CODEX**：平行改別的（寶箱/貓裝鍛造平衡等），改到 `monsterData.js`/`dungeonDb.js`/戰鬥相關檔案前先跟彼此對一下，避免 git 分岔。

## 已定案 / 待決
- ✅ 定案：四角分區、隊友格疊玩家卡上方、頂部資訊列、計分中央輸入、點隊友看詳情、前橘後藍、用原素材。
- ⏳ 待實機確認：中間動畫怎麼呈現、玩家卡/隊友框最終樣式、資訊列要不要再加(隊伍總傷害/計時)。
